---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString("en-gb", {
			weekday: "short",
			year: "numeric",
			month: "short",
			day: "2-digit",
		})
	}</time
>
